shader_type canvas_item;

uniform vec3 color;

void fragment() {
	
	// Place fragment code here.
	
	/* Pseudocode
		if selected width is past this pixels coordinate,  
		color it the new color, otherwise do nothing. 
		
		Or modulate the color in some way
	*/
}
